using UnityEngine;
using System.Collections;

public class TriggerScript : MonoBehaviour {
	
	// this component only tracks whether it has been triggered, so that other scripts can use it commonly
	public bool triggered;
}
